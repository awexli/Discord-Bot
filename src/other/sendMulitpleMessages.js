// var type = args[0];
// const amount = parseInt(args[1]);

// // first argument
// if (type == null) {
//   return message.reply(embed);
// }

// // second argument
// if (isNaN(amount)) {
//   return message.reply("that doesn't seem to be a valid number.");
// } else if (amount <= 1 || amount > 16) {
//   return message.reply('you need to input a number between 1 and 16.');
// }

// var count = amount;
// var array = [];
// if (type == 'haiku') {
//   for (var i = 0; i < count; i++) {
//     var textByLine = haikuText.split('\n');
//     var index = Math.floor(Math.random() * textByLine.length);
//     var html = 'https://youtu.be/' + textByLine[index];
//     array[i] = html;
//   }
// }

// message.channel.send(array);
